PORT=5000
MONGO_URI=mongodb://127.0.0.1:27017/medvault
SESSION_SECRET=super_secret_dev_key
# REVIEW: this is GROQ_KEY, but aiExplain.js/assistant.js/render.yaml all read
# GROQ_API_KEY (GROQ_KEY is only a fallback). Copying this file as-is leaves the
# AI features silently disabled.
GROQ_KEY=your_groq_api_key_here
